use trust_score_generators::exchange_application_tsg::{
    trivial_tsg
};
use utility::parse_messages;

use anyhow::Result;

mod trust_score_generators;
mod data_types;
mod tests;
mod utility;

fn main() -> Result<()> {
    return Ok(());
}
